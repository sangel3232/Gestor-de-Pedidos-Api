<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ============================================================
         001 - Esquema inicial: tipo enum, tabla cliente, tabla pedido
         ============================================================ -->

    <changeSet id="001-1" author="sergio" dbms="postgresql">
        <comment>Crear tipo ENUM estado_pedido en PostgreSQL</comment>
        <sql>
            DO $$ BEGIN
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'estado_pedido') THEN
                    CREATE TYPE estado_pedido AS ENUM ('CREADO', 'CONFIRMADO', 'PAGADO', 'CANCELADO');
                END IF;
            END $$;
        </sql>
        <rollback>
            DROP TYPE IF EXISTS estado_pedido;
        </rollback>
    </changeSet>

    <changeSet id="001-2" author="sergio">
        <comment>Crear tabla cliente</comment>
        <createTable tableName="cliente">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="nombre" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(150)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_cliente_email"/>
            </column>
            <column name="ciudad" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="activo" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="creado_en" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <rollback>
            <dropTable tableName="cliente"/>
        </rollback>
    </changeSet>

    <changeSet id="001-3" author="sergio">
        <comment>Índices sobre tabla cliente</comment>
        <createIndex indexName="idx_cliente_email" tableName="cliente">
            <column name="email"/>
        </createIndex>
        <createIndex indexName="idx_cliente_ciudad" tableName="cliente">
            <column name="ciudad"/>
        </createIndex>
        <rollback>
            <dropIndex indexName="idx_cliente_email" tableName="cliente"/>
            <dropIndex indexName="idx_cliente_ciudad" tableName="cliente"/>
        </rollback>
    </changeSet>

    <changeSet id="001-4" author="sergio" dbms="postgresql">
        <comment>Crear tabla pedido con FK a cliente y tipo enum</comment>
        <sql>
            CREATE TABLE IF NOT EXISTS pedido (
                id          BIGSERIAL           PRIMARY KEY,
                descripcion VARCHAR(255)        NOT NULL,
                total       NUMERIC(12, 2)      NOT NULL CHECK (total >= 0),
                estado      estado_pedido       NOT NULL DEFAULT 'CREADO',
                fecha       TIMESTAMP           NOT NULL DEFAULT NOW(),
                cliente_id  BIGINT              NOT NULL,
                CONSTRAINT fk_pedido_cliente
                    FOREIGN KEY (cliente_id)
                    REFERENCES cliente (id)
                    ON DELETE RESTRICT
            );
        </sql>
        <rollback>
            DROP TABLE IF EXISTS pedido;
        </rollback>
    </changeSet>

    <changeSet id="001-5" author="sergio">
        <comment>Índices sobre tabla pedido</comment>
        <createIndex indexName="idx_pedido_estado" tableName="pedido">
            <column name="estado"/>
        </createIndex>
        <createIndex indexName="idx_pedido_cliente_id" tableName="pedido">
            <column name="cliente_id"/>
        </createIndex>
        <createIndex indexName="idx_pedido_fecha" tableName="pedido">
            <column name="fecha" descending="true"/>
        </createIndex>
        <rollback>
            <dropIndex indexName="idx_pedido_estado"     tableName="pedido"/>
            <dropIndex indexName="idx_pedido_cliente_id" tableName="pedido"/>
            <dropIndex indexName="idx_pedido_fecha"      tableName="pedido"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
